#include <iostream>
using namespace std;

int main(void) {
    // 自分の得意な言語で
    // Let's チャレンジ！！
    string str = "hello";
    auto num = str.length();
    auto temp = str.at(num - 1);

    return 0;
}